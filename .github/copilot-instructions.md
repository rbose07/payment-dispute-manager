# Copilot Repository Instructions

Read and follow `AGENTS.md` for repository architecture, commands, agent boundaries, and OWASP audit rules.

This is a deliberately vulnerable training application. Do not silently harden or remediate security weaknesses unless the user explicitly requests a remediation exercise. When asked for an OWASP assessment, preserve the code and create only the requested report under `output/`.
